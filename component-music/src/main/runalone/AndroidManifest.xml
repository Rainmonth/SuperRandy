<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.rainmonth.music">

    <application
        android:name=".MusicApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/music_app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MusicAppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MusicSplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MusicMainActivity" />
        <activity android:name=".audioplayer.AudioPlayerActivity" />
        <activity android:name=".videoplayer.VideoPlayerActivity" />
        <activity android:name=".videoplayer.VideoPlayMainActivity" />

        <service android:name=".audioplayer.AudioPlayService" />
    </application>

</manifest>