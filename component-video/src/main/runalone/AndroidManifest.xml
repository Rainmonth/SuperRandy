<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.rainmonth.video">

    <application
        android:name=".VideoApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/video_app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".VideoLaunchActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GlobalVideoPlayerActivity" />
        <activity android:name=".ConfigVideoPlayActivity" />
        <activity android:name=".DetailVideoPlayActivity" />
        <activity android:name=".DyVideoPlayActivity" />
        <activity android:name=".FloatVideoPlayerActivity" />
        <activity android:name=".ListFocusVideoPlayActivity" />
        <activity android:name=".VideoMainActivity" />
        <activity android:name=".VideoPlayerActivity" />
    </application>

</manifest>